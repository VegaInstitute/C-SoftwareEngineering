#include <gtest/gtest.h>

#include "libcustommath/libcustom.hpp"


TEST(AddTests, Add1) {
  ASSERT_TRUE(add(1, 1) == 2);
}

TEST(AddTests, Add2) {
  ASSERT_TRUE(add(1, 1) == 2);
}

TEST(AddTests, Add3) {
  ASSERT_TRUE(add(1, 1) == 2);
}

TEST(AddTests, Add4) {
  ASSERT_TRUE(add(1, 1) == 2);
}

TEST(AddTests, Add5) {
  ASSERT_TRUE(add(1, 1) == 2);
}
